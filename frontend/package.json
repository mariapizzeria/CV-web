{
  "name": "cv-web",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc && npx live-server --port=8082 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  }
}
